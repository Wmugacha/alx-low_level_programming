#include "main.h"

/**
 * mul - function to multiply two integers.
 * @a: first integer.
 * @b: second integer.
 * Return: always 0.
 */

int mul(int a, int b)
{
	int prod;

	prod = (a * b);

	return (prod);
}
